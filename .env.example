# JobPilotAI — Environment Variables
# Copy this to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit your .env file. It is in .gitignore by default.

# ── AI Integration (OpenRouter) ──
# Get your key at: https://openrouter.ai/keys
# Supports 200+ models including Gemini, Claude, GPT via one API
OPENROUTER_API_KEY=

# ── Company Research (Brave Search) ──
# Free tier: 2,000 queries/month
# Get your key at: https://brave.com/search/api/
BRAVE_API_KEY=

# ── Email Notifications (optional) ──
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFICATION_EMAIL=

# ── API Keys for API-based boards (optional) ──
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
THEMUSE_API_KEY=
